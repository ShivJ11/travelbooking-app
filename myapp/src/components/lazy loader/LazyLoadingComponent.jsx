import React from 'react';
import './LazyLoadingComponent.css'; // Import CSS for animation

const LazyLoadingComponent = () => {
  return (
    <div className="lazy-loading">
      <div className="loader"></div>
    </div>
  );
};

export default LazyLoadingComponent;
